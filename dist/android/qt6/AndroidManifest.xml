<?xml version="1.0"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="gg.omega.tevolution"
          android:versionName=""
          android:versionCode=""
          android:installLocation="auto">

    <!-- %%INSERT_PERMISSIONS -->
    <!-- NOTE: This is useful to keep the application alive for Chromecast playback. -->
    <!-- NOTE: It seems we need to declare a service for this to have any effect. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- NOTE: This is useful if we want to open an image. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <!-- NOTE: This is useful if we want to write a file in DocumentsLocation. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- NOTE: This might be required for the camera. -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- %%INSERT_FEATURES -->

    <supports-screens android:anyDensity="true"
                      android:smallScreens="true"
                      android:normalScreens="true"
                      android:largeScreens="true"/>

    <application android:name="org.qtproject.qt.android.bindings.QtApplication"
                 android:label="-- %%INSERT_APP_NAME%% --"
                 android:extractNativeLibs="true"
                 android:hardwareAccelerated="true"
                 android:requestLegacyExternalStorage="true"
                 android:allowNativeHeapPointerTagging="false"
                 android:icon="@drawable/icon">

        <!-- FIXME Qt5: We need singleInstance and an empty taskAffinity for sharing to work properly. -->
        <activity android:name="gg.omega.WActivity"
                  android:label="-- %%INSERT_APP_NAME%% --"
                  android:configChanges="orientation|uiMode|screenLayout|screenSize|smallestScreenSize|layoutDirection|locale|fontScale|keyboard|keyboardHidden|navigation|mcc|mnc|density"
                  android:screenOrientation="unspecified"
                  android:launchMode="singleInstance"
                  android:taskAffinity=""
                  android:theme="@style/theme">

            <intent-filter>
                <action   android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- NOTE: Enable incoming URL. -->
            <intent-filter>
                <action   android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data     android:mimeType="*/*"/>
            </intent-filter>
            <!-- NOTE: Do we really need to support this ? -->
            <intent-filter>
                <action   android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data     android:mimeType="*/*"/>
                <data     android:scheme="file"/>
                <data     android:scheme="content"/>
            </intent-filter>

            <meta-data android:name="android.app.lib_name"  android:value="-- %%INSERT_APP_LIB_NAME%% --"/>
            <meta-data android:name="android.app.arguments" android:value="-- %%INSERT_APP_ARGUMENTS%% --"/>

            <meta-data android:name="android.app.extract_android_style" android:value="minimal"/>

            <meta-data android:name="android.app.splash_screen_drawable" android:resource="@drawable/splash"/>
            <meta-data android:name="android.app.splash_screen_sticky"   android:value="true"/>
        </activity>
        <!-- NOTE: This part is required for sharing files with other applications. -->
        <provider android:name="android.support.v4.content.FileProvider"
                  android:authorities="gg.omega.provider"
                  android:exported="false"
                  android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/paths"/>
        </provider>
    </application>
</manifest>
